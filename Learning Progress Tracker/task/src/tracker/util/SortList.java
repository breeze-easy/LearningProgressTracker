package tracker.util;
//
//import java.util.Arrays;
//import java.util.Collections;
//import java.util.Comparator;
//import java.util.List;
//
public class SortList {
//    public static void main(String[] args) {
//        // Define a list of integer arrays
//        int[][] list_of_arrays = {{3, 1, 4}, {1, 5, 9}, {2, 6, 5}, {3, 5, 8}};
//
////        // Define a custom comparator that sorts based on two elements
////        Comparator<int[]> customComparator = new Comparator<int[]>() {
////            public int compare(int[] a, int[] b) {
////                int compareFirst = Integer.compare(a[0], b[0]);
////                if (compareFirst != 0) {
////                    return compareFirst;
////                } else {
////                    return Integer.compare(a[1], b[1]);
////                }
////            }
////        };
//
//        // Sort the list of arrays using the custom comparator
//        Arrays.sort(list_of_arrays, customComparator);
//
//        // Print the sorted list
//        for (int i = 0; i < list_of_arrays.length; i++) {
//            System.out.println(Arrays.toString(list_of_arrays[i]));
//        }
//    }
//    List<int[]> sortIntArray (List<int[]> listToSort, List<int[]> sortParams ) {
//        //get sort parameters from sortParams[]
//        //sortParams.size is either 1 or 2. If 1 then sort only by one field, if 2 - then sort by 2
//        //1st element specifies element idx to compare
//        //2nd element specifies if sort should be in ascending or descending order: 0 - asc, 1 - desc
//
//
//        // Define a custom comparator that sorts based on two elements
//        Comparator<int[]> customComparator = new Comparator<int[]>() {
//            public int compare(int[] a, int[] b) {
//                int compareFirst = Integer.compare(a[0], b[0]);
//                if (compareFirst != 0) {
//                    return compareFirst;
//                } else {
//                    return Integer.compare(a[1], b[1]);
//                }
//            }
//        };
//    }
}
